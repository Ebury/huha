/* global jest, test, expect, beforeEach */

import Huha from '../src/huha';

// const mockSetUpEvents = jest.fn();

// jest.mock('../src/huha', () => jest.fn().mockImplementation(() => {
//   setUpEvents: mockSetUpEvents
// }));

// beforeEach(() => {
//   Huha.mockClear();
// });

test('new Huha object has no tasks', () => {
  //const huha = new Huha();
  //expect(huha.tasks).toHaveLength(0);
  expect(1 + 2).toBe(3);
});
